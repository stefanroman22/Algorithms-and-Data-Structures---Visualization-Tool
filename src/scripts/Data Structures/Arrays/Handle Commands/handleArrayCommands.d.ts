export declare function handleArrayCommands(
    command: string, 
    array: number[], 
    parseInputPressed: boolean,
    appendValue: Number,
    deleteIndex: Number,
    updateIndex: Number,
    updateValue: Number,
    searchValue: Number,
    setArray: (value: number[]) => void,
): void;
